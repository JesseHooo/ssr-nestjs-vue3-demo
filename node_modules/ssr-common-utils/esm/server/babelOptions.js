import { loadConfig } from './loadConfig';
export const getBabelOptions = ({ babel }) => {
    var _a;
    const { babelExtraModule, corejsOptions, babelOptions } = loadConfig();
    return [
        babel({
            babelHelpers: 'bundled',
            extensions: ['.ts', '.vue', '.tsx', '.js'],
            include: babelExtraModule.concat((_a = babelOptions === null || babelOptions === void 0 ? void 0 : babelOptions.include) !== null && _a !== void 0 ? _a : []),
            presets: [['@babel/preset-env', {
                        modules: false,
                        ...corejsOptions
                    }]]
        })
    ];
};
//# sourceMappingURL=babelOptions.js.map