"use strict";
const spinner = require('ora')('Starting');
process.on('message', (data) => {
    const { message } = data;
    if (message === 'start') {
        spinner.start();
    }
    else {
        spinner.stop();
        process.exit();
    }
});
//# sourceMappingURL=spinner.js.map